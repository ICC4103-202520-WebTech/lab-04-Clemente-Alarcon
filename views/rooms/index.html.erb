<h1>Rooms</h1>
<ul>
  <% @rooms.each do |room| %>
    <li>
      <%= link_to "Room ##{room.id}", room_path(room) %> —
      Number: <%= room.number %>,
      Type: <%= room.room_type %>,
      Price: <%= room.price %>,
      Status: <%= room.status %>
    </li>
  <% end %>
</ul>
